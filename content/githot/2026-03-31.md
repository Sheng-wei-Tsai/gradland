---
title: "GitHub Hot — 31 March 2026"
date: "2026-03-31"
excerpt: "Top 10 trending repos this week — use cases, how to use them, and project ideas for each."
tags: ["GitHub", "Open Source", "Tools"]
coverEmoji: "🔥"
auto_generated: true
---

*Top 10 repos trending on GitHub this week — what they do, why they matter, and how to use them in your projects.*

---

## 1. [larksuite/cli](https://github.com/larksuite/cli)

**4,741 stars this week** · Go

Official Lark/Feishu CLI with 200+ commands and 19 pre-built AI Agent Skills, letting you script or automate the entire Lark workspace from the terminal or an LLM agent.

**Use case**

If you're building an AI agent that needs to send messages, create docs, schedule meetings, or update spreadsheets in Lark/Feishu without hand-rolling OAuth flows and REST calls, this CLI handles all of that with structured, agent-optimized output. Concrete example: your AI blog assistant drafts a post, then automatically creates a Lark Doc, schedules a review meeting with collaborators, and sends a Messenger notification — all via chained CLI commands or a single Agent Skill invocation.

**Why it's trending**

The 'agent-native tooling' wave is peaking right now — developers are actively searching for pre-built tool layers they can drop into LangChain, Claude, or GPT function-calling setups without writing custom API wrappers. Lark/Feishu is dominant in Asia-Pacific tech companies, so this hits a large underserved developer audience.

**How to use it**

1. Install via npm: `npm install -g @larksuite/cli` (no Go toolchain needed for end users),2. Create a Lark app and authenticate interactively: `lark login` — it walks you through app creation and stores credentials in your OS keychain,3. Smoke test with a message: `lark message send --chat-id <CHAT_ID> --text 'Hello from CLI'`,4. Browse the 19 Agent Skills in the `/skills/` directory of the repo — each is a JSON/YAML schema you can paste directly into a LangChain Tool, OpenAI function definition, or Claude tool_use block,5. Wire a skill into your AI agent: load the skill schema, map the `command` field to a shell exec call, and let the LLM drive — e.g., the `docs.create` skill lets GPT-4 create and populate a Lark Doc with zero custom code

**How I could use this**

1. Blog publish pipeline: When Henry merges a post to main, a GitHub Action runs `lark docs create --title <post-title> --content <markdown>` to mirror every blog post as a Lark Doc — giving him a searchable knowledge base and a shareable link for Asian-market readers who live in Feishu.
2. Career tool integration: Build a 'weekly career digest' agent that pulls job leads Henry has bookmarked, runs them through his resume matcher, then uses `lark sheet append` to log scored results into a Feishu Base spreadsheet and `lark message send` to ping him a summary every Monday morning — all orchestrated from a single cron-triggered Node script.
3. AI writing assistant with human-in-the-loop review: Pipe Henry's AI blog draft generator output into `lark docs create`, then use `lark calendar event create` to automatically schedule a 30-minute self-review block, and `lark task create` to track edits needed — creating a zero-friction editorial workflow entirely driven by LLM output and CLI commands.

---

## 2. [magnum6actual/flipoff](https://github.com/magnum6actual/flipoff)

**2,520 stars this week** · JavaScript

A zero-dependency vanilla JS split-flap display emulator that turns any browser into a retro airport departure board — free alternative to $3,500 hardware.

**Use case**

Developers building event displays, office dashboards, or portfolio sites want the retro split-flap aesthetic without hardware costs or heavy dependencies. Concretely: you want a live 'what's deploying now' board on your office TV, or a visually striking hero element on a portfolio site that cycles through taglines with authentic flip animations and clacking sounds.

**Why it's trending**

Retro UI aesthetics are having a strong moment in 2024-2025 developer design culture, and this hits a specific niche — it's a polished, zero-install single-file implementation that scratches the nostalgia itch for conference room displays and personal sites without any npm install. The viral factor is the price contrast: $0 vs $3,500.

**How to use it**

1. Clone the repo: `git clone https://github.com/magnum6actual/flipoff && cd flipoff`
2. Swap out the hardcoded quotes array in `index.html` with your own strings — search for the messages/quotes array near the top of the script block.
3. Serve it: `python3 -m http.server 8080` then open `http://localhost:8080`
4. Embed it in a Next.js page using an `<iframe src='/flipoff/index.html' />` pointed at a static asset in your `/public` folder, or wrap the core tile animation logic in a React component using `useEffect` to drive character cycling.
5. Press `F` for fullscreen — wire this to a button in your UI via `document.getElementById('board').requestFullscreen()` if embedding.

**How I could use this**

1. Use it as the hero section of Henry's blog: cycle through 4-5 one-liners like 'FULL STACK ENGINEER', 'NEXT.JS + SUPABASE', 'OPEN TO WORK' — drop the raw `index.html` into `/public/flipboard/` and embed it as a full-width iframe above the fold. It'll stop scrollers cold.
2. Build a 'Skills Departures Board' for the portfolio/resume page: each row shows a skill and proficiency level (e.g. 'TYPESCRIPT........EXPERT', 'PYTHON..........INTERMEDIATE') that flips in on page load. Wire it to pull from a Supabase table so Henry can update skills without redeploying.
3. Pipe AI-generated content through it: after a blog post publishes, trigger a Supabase edge function that calls OpenAI to generate a 40-character summary, stores it, then the flipboard on the homepage fetches the latest post summaries via a `/api/latest-posts` route and cycles through them — a live, animated 'departures board' of recent writing.

---

## 3. [elder-plinius/G0DM0D3](https://github.com/elder-plinius/G0DM0D3)

**2,259 stars this week** · TypeScript

A single-file, open-source multi-model AI chat UI with red-teaming tools, parallel model racing, and jailbreak research features built on OpenRouter's 55+ model catalog.

**Use case**

When you're building AI features and need to benchmark prompt quality across multiple models simultaneously — without paying for 5 separate API dashboards or trusting a vendor's chat UI with your keys. Concrete example: you're writing a blog post summarizer and want to see how GPT-4o, Claude Sonnet, and Mistral handle the same article in parallel before committing to one provider. GODMODE CLASSIC runs all five combos and scores them, saving hours of manual copy-paste testing.

**Why it's trending**

Post-training 'alignment tax' discourse is peaking in AI circles right now, and this repo explicitly targets researchers and builders who want to probe model behavior without corporate guardrails — it's a lightning rod for that community. The GPT-5 drop and OpenRouter's expanding catalog also made a 55-model parallel evaluation tool immediately useful for anyone doing model selection.

**How to use it**

1. Clone or just download the single `index.html` file — no Node, no build step: `curl -O https://raw.githubusercontent.com/elder-plinius/G0DM0D3/main/index.html`. 2. Open it in any browser and paste your OpenRouter API key (it stays in localStorage, never leaves your machine). 3. To run parallel model comparisons, switch to GODMODE CLASSIC mode, type your prompt, and watch 5 model+system-prompt combos race and score against each other. 4. For prompt robustness testing, use the Parseltongue engine — pick an intensity tier (1-3) and it applies up to 33 text perturbation techniques to your input to see how model responses degrade. 5. To self-host on your Next.js blog, drop the `index.html` into `/public/godmode/` and link to it, or serve it from a Vercel edge route — zero backend required since all API calls go client-side to OpenRouter.

**How I could use this**

1. Build a 'Best AI Answer' blog widget: when Henry publishes a post with a key question (e.g. 'What's the best TypeScript ORM in 2025?'), embed a GODMODE CLASSIC-style parallel query against 5 models and display the composite-scored consensus answer inline — updated live so the post stays evergreen without manual edits.
2. Use the Parseltongue perturbation engine as a resume/cover-letter stress tester: feed a job description + cover letter draft through 3 intensity tiers of input noise and measure how well the AI coaching response holds up — if the advice changes dramatically under mild perturbation, the prompt engineering is fragile and needs hardening before shipping to users.
3. Wire ULTRAPLINIAN's multi-tier model evaluation into Henry's Supabase backend as a prompt quality audit pipeline: whenever a new AI-generated blog post draft is saved, trigger a serverless function that runs the draft's key claims through 10 models, stores composite agreement scores in a `post_confidence` table, and surfaces a 'AI Consensus Score' badge on the published post — a genuine differentiator that shows editorial rigor.

---

## 4. [TheTom/turboquant_plus](https://github.com/TheTom/turboquant_plus)

**2,012 stars this week** · Python

TurboQuant+ compresses transformer KV caches 3.8–6.4x using quantization + attention-aware sparse decoding, letting you run large LLMs locally with near-native speed on Apple Silicon.

**Use case**

Running a 35B MoE model locally on an M-series Mac normally hits a wall at long contexts — the KV cache balloons in memory and decode throughput tanks. TurboQuant+ solves this by swapping in turbo3/turbo2 cache formats via llama.cpp flags, giving you 4.6–6.4x memory reduction and up to +22.8% decode speed at 32K context with ~1% perplexity cost — meaning you can actually chat with a 35B model at long context on an M5 Max without quantizing the weights further.

**Why it's trending**

It implements an ICLR 2026 paper that dropped this week with a working llama.cpp integration and Metal GPU kernels — rare combination of cutting-edge research with immediately usable hardware support for the large Apple Silicon developer community. The 'Sparse V' attention-gating trick on top of the paper is the novel community contribution drawing extra attention.

**How to use it**

1. Build llama.cpp from the fork referenced in the repo that includes the C port with Metal kernels (follow the standard llama.cpp cmake build, the turbo cache types are registered as first-class cache formats).
2. Pull a supported model (validated on Qwen 3.5 35B-A3B GGUF) and run inference with the new cache flags:
```bash
./llama-cli \
  -m qwen3.5-35b-a3b.gguf \
  --cache-type-k turbo3 \
  --cache-type-v turbo3 \
  -c 32768 \
  -p "Your prompt here"
```
3. Benchmark perplexity delta vs q8_0 using the provided diagnostics suite (`pytest tests/ -v`) to validate quality on your target domain.
4. If on M5 Max, enable turbo2 for maximum 6.4x compression — monitor PPL increase vs your acceptable threshold.
5. Layer in Sparse V (enabled by default in the patched build) and measure decode tokens/sec at 32K vs baseline to confirm the +22% speedup on your hardware.

**How I could use this**

1. Run Henry's blog AI assistant (summarization, Q&A over posts) locally on an M-series Mac using a 35B MoE model with turbo3 KV cache — this makes a genuinely capable model viable for always-on local inference without cloud API costs, and he can write a concrete benchmark post comparing turbo2/turbo3/q8_0 perplexity vs speed tradeoffs on real blog content.
2. Build a local resume/cover-letter generation pipeline using a 35B model that stays fully private — turbo3 compression means the model fits in the memory budget of a 64GB Mac alongside the Next.js dev server, making a zero-API-cost career tool where sensitive resume data never leaves the machine.
3. Add a long-context 'chat with my entire blog archive' feature: embed all posts into a 32K+ context window and use the compressed KV cache to maintain that context affordably during multi-turn Q&A — the Sparse V speedup specifically targets the long-context decode bottleneck that makes this UX pattern feel sluggish today.

---

## 5. [openai/codex-plugin-cc](https://github.com/openai/codex-plugin-cc)

**1,785 stars this week** · JavaScript

An official OpenAI plugin that lets you run Codex code reviews and delegate background tasks directly inside Claude Code's slash-command workflow.

**Use case**

If you're already using Claude Code as your primary AI coding assistant but want OpenAI's Codex for a second-opinion review or to offload a long-running refactor task, this plugin bridges the two without context-switching. Concrete example: you've just wired up a new Supabase RLS policy across 8 files — run `/codex:adversarial-review` to get a challenge-mode review that actively looks for security holes Claude Code might have glossed over.

**Why it's trending**

It dropped the same week tensions around Claude vs. OpenAI tooling peaked in the dev community — using an official OpenAI plugin *inside* Anthropic's Claude Code is a provocative workflow that's generating genuine curiosity. It also makes Codex practically accessible to ChatGPT Free users for the first time, lowering the barrier significantly.

**How to use it**

1. Inside Claude Code, run `/plugin marketplace add openai/codex-plugin-cc` then `/plugin install codex@openai-codex` and `/reload-plugins`.
2. Run `/codex:setup` — it will detect if `@openai/codex` is missing and offer to install it via npm automatically.
3. If not logged in, run `!codex login` to authenticate with your OpenAI account or API key.
4. Kick off a background review of your current uncommitted changes: `/codex:review --background`, then poll with `/codex:status` and retrieve output with `/codex:result`.
5. For adversarial reviews (great for security-sensitive code like auth flows), use `/codex:adversarial-review` — it steers Codex to actively challenge assumptions rather than just summarize.

**How I could use this**

1. Wire `/codex:adversarial-review` into a pre-commit hook on your blog's repo that targets changed files in `app/api/` — any time you modify Supabase Edge Function handlers or auth routes, you get an automatic adversarial pass before the commit lands, surfacing prompt-injection or RLS bypass risks specific to your AI blog's data model.
2. Build a 'dual-LLM review' section in your portfolio site: when you push a new project, a GitHub Action triggers both Claude's review and Codex via this plugin in background mode, then your blog's admin page surfaces both reviews side-by-side using `/codex:result` output — a concrete, visible demonstration of multi-model AI workflows that hiring managers can see in action.
3. Use `/codex:rescue` as a fallback agent in your blog's AI writing assistant: when Claude Code gets stuck on a complex TypeScript inference issue or a gnarly Supabase query optimization, delegate the blocked task to Codex in the background with a structured prompt, poll `/codex:status` via a small Node script, and surface the result back into your editor — effectively giving yourself a two-model problem-solving pipeline without leaving your workflow.

---

## 6. [qiuzhi2046/Qclaw](https://github.com/qiuzhi2046/Qclaw)

**1,297 stars this week** · TypeScript

Qclaw is a GUI desktop wrapper (Electron + React) around OpenClaw CLI that lets non-technical users configure and run AI agents without touching a terminal.

**Use case**

Running OpenClaw (an open-source AI agent framework) normally requires CLI setup, environment variables, and manual Node.js wrangling — a real barrier for non-developers or rapid prototyping. Qclaw wraps all of that in a visual config panel, so you can wire up Feishu/WeChat integrations, swap models, and manage agent state without writing shell commands. For example: spin up a customer-support AI bot connected to WeChat in under 5 minutes by filling out a form instead of editing JSON configs.

**Why it's trending**

OpenClaw is gaining momentum in the Chinese AI developer ecosystem as a domestic alternative to LangChain-style agent frameworks, and Qclaw is riding that wave by dramatically lowering the entry barrier — 1,297 stars in a single week suggests a large audience that wanted exactly this GUI abstraction.

**How to use it**

1. Download the latest release binary from https://qclawai.com/ for your OS (macOS/Windows/Linux builds are provided — no manual build required).,2. On first launch, Qclaw auto-detects whether Node.js and the OpenClaw CLI are installed; if not, hit 'Auto Install' in the environment check screen and it handles the setup.,3. In the 'Config' panel, select your model (supports all OpenClaw models plus custom endpoints — paste your API key and base URL for OpenAI-compatible providers).,4. To connect an IM channel (e.g. WeChat), navigate to the 'IM Plugins' tab, select WeChat, and scan the QR code — the agent is now live on that channel.,5. For local development/contribution: `git clone https://github.com/qiuzhi2046/Qclaw && cd Qclaw && pnpm install && pnpm dev` — it's a standard Electron-Vite-React stack you'll recognize immediately.

**How I could use this**

1. Wire Qclaw's WeChat IM plugin to a dedicated 'Ask My Blog' agent: configure an OpenClaw agent with your blog's Supabase content as context, so readers can WeChat-message questions and get answers sourced directly from your published posts — zero backend code from you.
2. Use Qclaw locally as a no-friction testbed for agent workflows before porting them to your Next.js blog's API routes — prototype a 'resume gap analyzer' agent in Qclaw's GUI (swap prompts, test model responses), then extract the finalized prompt chain into a /api/analyze-resume Edge Function once it's stable.
3. Build a blog post pipeline agent inside Qclaw: configure it to accept a raw draft (pasted into a Feishu doc), run it through a multi-step OpenClaw chain (SEO title generation → summary → tag extraction → readability score), and write the structured output back to a Supabase table that your Next.js blog reads from — effectively a no-CLI editorial automation layer.

---

## 7. [yetone/voice-input-src](https://github.com/yetone/voice-input-src)

**1,055 stars this week** · various

A Claude-generated macOS menu-bar app that lets you hold Fn to dictate text anywhere on your Mac — the repo is literally the prompt used to vibe-code it with Claude CLI, not the app itself.

**Use case**

Developers who write a lot — blog posts, commit messages, Slack replies — waste time switching between keyboard and thought. This solves the 'I want to speak my first draft and have it land in whatever text field is focused' problem without opening a separate app. Concrete example: you're in VS Code writing a docstring, hold Fn, speak the description, release, and it pastes the transcription inline.

**Why it's trending**

It went viral not because of the app, but because it demonstrates Claude CLI's --dangerously-skip-permissions mode shipping a complete, production-quality Swift/macOS app from a single massive prompt — it's a benchmark moment for agentic coding tools that people are stress-testing right now.

**How to use it**

1. Install Claude CLI and ensure you have API access: `npm install -g @anthropic-ai/claude-cli && claude auth login`.
2. Clone the repo to get the prompt: `git clone https://github.com/yetone/voice-input-src && cat README.md` — copy the full prompt text.
3. Run Claude against it: `claude --dangerously-skip-permissions --output-format=stream-json --verbose -p "<paste full prompt here>"` — Claude will scaffold and write all Swift files.
4. Open the generated Xcode project, set your Team signing in the target settings, and build to your Mac (macOS 14+ required).
5. Grant Accessibility and Microphone permissions on first launch — the app appears in your menu bar; hold Fn anywhere to dictate.

**How I could use this**

1. Wire this into your Next.js blog's admin editor (the one you write posts in) by pairing it with a custom Tiptap or textarea component — speak a rough paragraph, let it land, then trigger an OpenAI /api/polish endpoint on blur to clean it up. Zero context switching between thinking and typing.
2. Build a voice-driven cover letter micro-tool: hold Fn, speak bullet points about a job you're applying to, have the transcription feed a Supabase edge function that chunks your existing resume data and generates a tailored letter — the voice input removes the friction of typing job details into a form.
3. Add a 'voice memo to blog post' pipeline in your Supabase backend: transcription text POSTs to a /api/draft endpoint that uses GPT-4o to expand spoken notes into a structured MDX draft, stores it in your posts table with status='draft', and surfaces it in your admin dashboard — lets you capture ideas while walking without ever opening a laptop.

---

## 8. [WecomTeam/wecom-cli](https://github.com/WecomTeam/wecom-cli)

**1,009 stars this week** · Rust

A Rust-powered CLI that exposes WeCom (Enterprise WeChat) APIs as shell commands and pre-built AI Agent skills, letting both humans and LLM agents automate messaging, calendars, docs, and more without writing API glue code.

**Use case**

Teams using WeCom as their internal comms platform need to automate workflows — sending notifications, creating todos from meeting notes, or querying org charts — but the REST API requires custom auth and serialization for every call. This CLI wraps all of that into single-command invocations (e.g. `wecom-cli todo create '{"title":"Review PR"}'`) and ships ready-made skill definitions so an AI agent like Claude or GPT can call these tools directly via function calling or MCP, no extra integration layer needed.

**Why it's trending**

The AI agent tooling space is exploding right now and WeCom (used by 180M+ enterprise users in China) has had no clean CLI/agent interface until this. Its explicit design around agent 'Skills' compatible with mainstream AI frameworks makes it immediately relevant to anyone building LLM-powered workplace automation.

**How to use it**

1. Install the CLI and skills: `npm install -g @wecom/cli && npx skills add WeComTeam/wecom-cli -y -g`
2. Configure your WeCom Bot credentials once: `wecom-cli init` (interactive prompt for Bot ID + Secret)
3. Test a basic API call: `wecom-cli contact get_userlist '{}'` — returns visible org member list as JSON
4. Wire it into your AI agent by pointing your agent framework (e.g. Claude Desktop, Continue.dev, or a custom MCP server) at the installed skills directory — the skill manifests describe inputs/outputs so the LLM can call them natively
5. Automate a real workflow: `wecom-cli todo create '{"title":"Deploy blog","assignee":"henry_userid"}'` — pipe outputs into your own scripts or let an agent chain calls together

**How I could use this**

1. Build a 'publish and notify' Next.js API route: when Henry publishes a new blog post in Supabase, a server action calls `wecom-cli message send` via Node's `child_process` to push the post title + URL to a WeCom group bot — zero third-party SaaS needed for internal notifications.
2. Create a career dashboard integration: use the todos and calendar skills to let an AI agent auto-create WeCom todos whenever a job application status changes in a Supabase `applications` table — the agent reads the DB change via a Supabase realtime subscription and calls `wecom-cli todo create` with deadline and assignee pre-filled.
3. Expose the skills as an MCP tool server for Henry's blog's AI assistant: when a reader asks 'what is Henry working on this week?' the assistant queries `wecom-cli schedule get_list` and `wecom-cli todo list` in real-time and synthesizes a human-readable answer, giving the blog an 'ask me anything about my current work' feature backed by live WeCom data.

---

## 9. [jxnxts/mcp-brasil](https://github.com/jxnxts/mcp-brasil)

**944 stars this week** · Python · `ai-agents` `apis-publicas` `brazil` `claude`

A plug-and-play MCP server that gives Claude and other LLMs direct access to 326 tools across 41 Brazilian public government APIs — no auth required for most of them.

**Use case**

Brazilian developers building AI agents need to query government data (federal spending, congressional votes, court rulings, electoral results, environmental data) but these APIs have inconsistent interfaces, no unified auth, and poor discoverability. This repo wraps all of them into a single MCP server so an LLM can autonomously plan and execute cross-API queries — e.g., ask Claude 'show me all expenses and voting record for deputy X this year' and it auto-chains gastos + votações + proposições APIs in parallel.

**Why it's trending**

MCP (Model Context Protocol) exploded in adoption this month as the standard way to give LLMs tool access, and this is one of the first comprehensive MCP servers for a non-English government data ecosystem — 944 stars in a week signals strong demand from the Brazilian dev community building local AI agents.

**How to use it**

Install with `pip install mcp-brasil` or `uv add mcp-brasil`,Add to Claude Desktop's config file at `~/Library/Application Support/Claude/claude_desktop_config.json` with the JSON snippet from the README — no API keys needed to start since 38/41 APIs are open,Test immediately by asking Claude: 'Quais foram os gastos do deputado federal [nome] no último mês?' — Claude will use `planejar_consulta` to build a multi-API execution plan automatically,For programmatic use in your Next.js blog's backend, call the MCP server as a subprocess or use the Python client: `from mcp_brasil import MCPBrasil; client = MCPBrasil()` and invoke tools like `client.execute('buscar_deputado', {'nome': 'João Silva'})`,To cross-reference data (e.g., politician's spending + voting + proposals), let the agent use `executar_lote` — it fires all sub-queries in parallel and merges results in one response

**How I could use this**

1. Build a 'Brazilian Politics Tracker' blog series where a Claude agent auto-generates weekly posts combining a specific deputy's spending data, voting record, and active propositions — fully sourced from live government APIs, giving your blog unique data journalism content no one else is publishing in English or PT
2. Create a portfolio piece: a 'Transparency Dashboard' side project that uses mcp-brasil + Supabase to cache and surface federal procurement data (compras públicas), letting visitors search which companies won government contracts by CNPJ — a concrete, impressive demo of AI + civic tech that stands out in a dev portfolio
3. Add an AI chat widget to your blog that answers Brazilian tech/economy questions using real-time data — e.g., 'What's the current Selic rate?' or 'Who voted against the AI regulation bill?' — grounded in live government sources instead of hallucinated LLM knowledge, demonstrating RAG-with-live-APIs as a blog feature

---

## 10. [oaker-io/wewrite](https://github.com/oaker-io/wewrite)

**940 stars this week** · Python

WeWrite is a Claude Code skill that automates the entire Chinese WeChat public account article pipeline — from trending topic scraping to draft publishing — with AI detection evasion built in.

**Use case**

Running a content-heavy blog or newsletter solo means spending hours on topic research, writing, SEO, and formatting separately. WeWrite collapses this into a single prompt: it scrapes Weibo/Baidu trending topics, scores and selects one, pulls real WebSearch citations, writes with your personal style fingerprint injected, generates cover images, applies WeChat-compatible formatting, and pushes directly to your draft box — all without switching tools.

**Why it's trending**

Claude Code's skill/tool-use ecosystem is exploding right now as developers realize you can wrap entire multi-step workflows as reusable agent skills rather than one-shot prompts. WeWrite is one of the first production-grade examples of this pattern applied to a real publishing workflow, making it a concrete reference architecture for anyone building Claude Code skills.

**How to use it**

1. Install the skill into Claude Code by cloning the repo and pointing Claude Code at SKILL.md: `claude install ./wewrite/SKILL.md`
2. Run the one-time setup to define your blog's voice and connect your WeChat API credentials in `style.yaml` (set `writing_persona: 'midnight-friend'` for personal blog tone)
3. Import your existing published posts to build your style fingerprint: `python scripts/extract_exemplar.py --posts ./my_articles/`
4. Trigger the full pipeline in Claude Code chat: 'Write a WeChat article about [topic or just say 写一篇公众号文章]'
5. Review the 2-3 editorial anchor points the draft flags, add your own voice in those spots, then approve the draft push to WeChat — total human time: ~5 minutes

**How I could use this**

1. Adapt the `extract_exemplar.py` style fingerprinting script for Henry's Next.js blog: parse his existing blog posts to extract sentence-length rhythms, transition patterns, and emotional tone markers, then inject this 'style DNA' as a system prompt prefix whenever he drafts new posts via an AI writing assistant endpoint in his Supabase-backed blog CMS.
2. Port the topic scoring system (`references/topic-selection.md` — 10 topics × 3 scoring dimensions + historical deduplication) into a career tool: scrape HN, Dev.to, and LinkedIn trending topics weekly, score them against Henry's existing portfolio topics, and surface the highest-signal gaps he hasn't written about yet — stored in Supabase with a simple Next.js dashboard.
3. Build a Claude Code skill for Henry's own blog using the same SKILL.md pattern: a reusable agent that takes a raw idea, runs it through a condensed version of this pipeline (WebSearch for real citations → outline → draft with editorial anchors → auto-generate og:image prompt → push to Supabase draft table), making it invokable from the terminal whenever he wants to write without context-switching into a GUI.
